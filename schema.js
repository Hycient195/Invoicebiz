user = {
  first_name,
  last_name,
  avatar,
  email,
  phone_number,
  timestamp
}

feed = {
  category,
  time_generated,
  user_id,
  timestamp
}

account = {
  balance,
  overdue_amount,
  owing_amount,
  invoiced_amount,
  amount_paid,
  user_id,
  timestamp
}

invoices = {
  client_name,
  issue_date,
  due_date,
  amount,
  status,
  user_id,
  timestamp
}